# Batch 1.3 — Content editor dirty-state protection

**Phase:** 1 · **Est. context:** ~22k · **Depends on:** none (parallel with 1.1 after ledger allows)

## Read only

1. `src/routes/_authenticated/content.tsx` (focus: ~136–168)
2. Phase 1 excerpt from remediation plan

## Drift check

```bash
git diff --stat a0b1585..HEAD -- src/routes/_authenticated/content.tsx
```

## Tasks

- [ ] Rehydrate `editTitle` / `editData` only when: selected entry id changes, no dirty local edits, or after save/discard.
- [ ] Do not overwrite dirty state on ordinary Convex query refreshes.

## STOP

If dirty-state needs content history/publish semantic changes → `blocked` in ledger.

## Verification

`bun run test`

## On completion

Ledger: 1.3 → `done`.
