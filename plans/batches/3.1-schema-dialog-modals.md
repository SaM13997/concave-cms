# Batch 3.1 — Schema Dialog modals

**Model: composer-2.5 only**

## Scope

- `src/routes/_authenticated/schema.tsx`
- `src/components/ui/` (add Dialog if missing)

## Tasks

1. Replace hand-rolled modals on schema page with shadcn `Dialog` pattern used elsewhere.
2. Preserve apply/discard/validation UX contract (`docs/schema-apply-ux-contract.md`).

## Verify

`bun run test` and `bun run test:e2e -- e2e/schema-builder.spec.ts`

## Done

Commit+push `dev-agent`; ledger batch 3.1 → `done`.
