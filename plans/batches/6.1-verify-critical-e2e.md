# Batch 6.1 — Verify script + critical E2E subset

**Phase:** 6 · **Est. context:** ~24k · **Depends on:** Phase 5 complete

## Read only

1. `package.json` scripts
2. Existing `e2e/*.spec.ts` from phases 1–5

## Tasks

- [ ] Decide: full Playwright in `verify` vs `test:e2e:critical` subset.
- [ ] Add `test:e2e:critical` if needed covering: schema typing, content dirty-state, JSON/select, apply validation/conflict, nav/RBAC, media/settings honesty.
- [ ] Wire into `verify` or document intentional exception with owner.

## Verification

```bash
bun run test
bun run test:e2e:critical   # or documented equivalent
```

## On completion

Ledger: 6.1 → `done`.
