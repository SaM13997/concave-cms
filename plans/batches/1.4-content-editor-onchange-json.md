# Batch 1.4 — ContentEntryEditor onChange + JSON editing

**Phase:** 1 · **Est. context:** ~24k · **Depends on:** none

## Read only

1. `src/components/content/ContentEntryEditor.tsx` (focus: ~245–253, ~290)
2. Phase 1 checklist items for JSON and functional updates

## Drift check

```bash
git diff --stat a0b1585..HEAD -- src/components/content/ContentEntryEditor.tsx
```

## Tasks

- [ ] Field changes use functional updates: `prev => ({ ...prev, [field.slug]: value })`. Extend `onChange` to accept object or updater callback if needed.
- [ ] JSON fields: separate display text from parsed value; keep invalid intermediate text visible; inline parse errors; call parent `onChange` only on valid JSON.
- [ ] Preserve all field types: text, richtext, number, boolean, date, select, json, image, reference.

## Verification

`bun run test`

## On completion

Ledger: 1.4 → `done`.
