# Batch 1.1 — Navigation consistency

**Model: composer-2.5 only** — do not use composer-2.5-fast or any other model.

## Read order

1. `orchestration.md`
2. `plans/ux-ui-remediation-ledger.md`
3. This file

## Scope (only these files)

- `src/config/navigation.ts`
- `src/components/BottomNav.tsx`
- `src/routes/_authenticated/index.tsx`
- `src/hooks/use-keyboard-navigation.ts` (or equivalent keyboard nav hook)
- `e2e/navigation.spec.ts`
- `e2e/keyboard.spec.ts` (if shortcuts change)

## Tasks

1. **Add Media to bottom nav** — `/media` is reachable from home/Cmd+K but missing from `bottomNavItems`. Add with appropriate icon, colors, and `content:read` permission (match existing patterns).
2. **Replace dashboard `<a href>` with TanStack `Link`** on the home dashboard cards so navigation stays SPA-style (view transitions, no full reload).
3. **Extend `g`+ keyboard shortcuts** — today only home/content/schema; add shortcuts for audit, settings, and media consistent with `use-keyboard-navigation.ts` and `e2e/navigation.spec.ts` patterns.
4. **Update E2E** — ensure navigation spec covers media in bottom nav and any new shortcuts.

## Out of scope

- RBAC settings visibility (batch 1.2)
- Page layout / max-width (batch 1.3)
- Debug nav gating (batch 2.2)

## Verify

```bash
bun run test
bun run test:e2e -- e2e/navigation.spec.ts
bun run test:e2e -- e2e/keyboard.spec.ts
```

Never run `scripts/e2e-server.sh`.

## Done checklist

- [ ] Changes committed and pushed to `dev-agent`
- [ ] Ledger updated on `cursor/ux-ui-remediation-orchestration-6b85`: batch 1.1 → `done`, clear `active_agent`, log entry
- [ ] If blocked, set batch 1.1 → `blocked` with reason and clear `active_agent`
