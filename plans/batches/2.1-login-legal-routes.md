# Batch 2.1 — Login legal routes

**Model: composer-2.5 only**

## Scope

- `src/components/login-form.tsx`
- `src/routes/terms.tsx` (new)
- `src/routes/privacy.tsx` (new)

## Tasks

1. Add minimal `/terms` and `/privacy` routes (or remove links if product prefers).
2. Prefer simple static content pages matching admin dark theme.

## Verify

`bun run test` and `bun run test:e2e -- e2e/auth.spec.ts`

## Done

Commit+push `dev-agent`; ledger batch 2.1 → `done`.
