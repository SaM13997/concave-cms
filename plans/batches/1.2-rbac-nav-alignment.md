# Batch 1.2 — RBAC nav alignment

**Model: composer-2.5 only**

## Read order

`orchestration.md` → `plans/ux-ui-remediation-ledger.md` → this file

## Scope

- `src/config/navigation.ts`
- `src/routes/_authenticated/settings.tsx`
- `e2e/rbac.spec.ts`

## Tasks

1. Settings nav item should require **admin** role (or equivalent permission), not merely `schema:read`, so non-admins do not see Settings in bottom nav when they cannot use exports.
2. Align `navItemsForPermissions` / RBAC tests with the change.
3. Update `e2e/rbac.spec.ts` for settings visibility.

## Verify

`bun run test` and `bun run test:e2e -- e2e/rbac.spec.ts`

## Done

Commit+push `dev-agent`; ledger batch 1.2 → `done` on orchestration branch.
