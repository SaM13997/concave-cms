# Batch 3.3 — Validation errors + apply progress UI

**Phase:** 3 · **Est. context:** ~26k · **Depends on:** 3.2

## Read only

1. `src/routes/_authenticated/schema.tsx` (apply UI)
2. `docs/schema-apply-ux-contract.md` — error display + progress sections

## Tasks

- [ ] Separate progress labels: `Validating schema...` vs `Applying changes...`
- [ ] Server validation errors in `data-testid="schema-validation-errors"` with focus links to controls.

## Verification

`bun run test`

## On completion

Ledger: 3.3 → `done`.
