# Batch 6.2 — Lint baseline + agent testing docs

**Phase:** 6 · **Est. context:** ~20k · **Depends on:** 6.1

## Read only

1. `docs/agent-testing.md`
2. `package.json`, CI config if present

## Tasks

- [ ] Document E2E prerequisites and critical-path commands in `docs/agent-testing.md`.
- [ ] Biome backlog: fix enough for meaningful `bun run check` **or** document separate lint-baseline follow-up (do not pretend verify is green).
- [ ] Update CI / release verification docs if `.github/workflows` exists.

## STOP

Biome backlog too large → document follow-up only; do not mass-format unrelated files.

## Verification

```bash
bun run verify   # or documented exception
```

## On completion

Ledger: 6.2 → `done`. Remediation program complete when all batches `done`.
