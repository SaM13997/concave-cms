# Batch 1.2 — Select field options editor

**Phase:** 1 · **Est. context:** ~18k · **Depends on:** 1.1

## Read only

1. `src/routes/_authenticated/schema.tsx` — field type `select` rendering
2. Batch 1.1 changes (git diff)

## Drift check

```bash
git diff --stat a0b1585..HEAD -- src/routes/_authenticated/schema.tsx
```

## Tasks

- [ ] When `field.type === "select"`, add UI to configure options (labels/values) before content use.
- [ ] Persist options through same draft-buffer pattern from 1.1.

## Verification

`bun run test` · `bun run check` (informational)

## On completion

Update ledger: 1.2 → `done`.
