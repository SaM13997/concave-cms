# Batch 2.1 — Compatibility policy + contentValidation

**Phase:** 2 · **Est. context:** ~28k · **Depends on:** Phase 1 complete

## Read only

1. `convex/lib/contentValidation.ts` (~196–204)
2. `docs/design.md` — skim ADR-003 / runtime-generic validation
3. Phase 2 checklist in remediation plan

## Drift check

```bash
git diff --stat a0b1585..HEAD -- convex/lib/contentValidation.ts
```

## Tasks

- [ ] Document chosen policy in code comment or short `docs/` note: preserve legacy unknown keys vs strip vs migration.
- [ ] **Prefer:** tolerate stored unknown keys during validation; validate only active schema fields for normal saves.
- [ ] Update validation so `UNKNOWN_FIELD` does not block edits when keys are legacy from deleted/renamed fields.

## STOP

If policy would hide data deletion from admins → `blocked`.

## Verification

`bun run test` · add/update `convex/lib/contentValidation.test.ts` stubs if needed (full tests in 2.5)

## On completion

Ledger: 2.1 → `done`. Record chosen policy in ledger log.
