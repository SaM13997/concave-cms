# Batch 3.1 — Apply state transitions (server)

**Phase:** 3 · **Est. context:** ~28k · **Depends on:** Phase 2 complete

## Read only

1. `convex/schemas.ts` (~702–705, apply mutation flow)
2. `docs/schema-apply-ux-contract.md`

## Tasks

- [ ] Validation failure → remain/return `draft` with errors (not `apply_failed`).
- [ ] Server/network interruption may use `apply_failed`.
- [ ] Success promotes draft → active.

## STOP

If server cannot distinguish validation vs interruption → `blocked`, document subplan.

## Verification

`bun run test`

## On completion

Ledger: 3.1 → `done`.
